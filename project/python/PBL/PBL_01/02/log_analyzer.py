# --------------------------------------------------------------------------------------------------------------------------------------------

# 생성형 AI 활용을 위한 파이썬 2번 문제 - 로그 파일을 활용한 IP 접속 분석

# 문제 상황
# 서버 보안 분석을 위해 로그 파일에서 접속한 IP 주소를 파악해야 하며, 이를 통해 자주 접속한 IP를 분석하고 보고해야 합니다.

# 문제 정의 및 요구사항
# - 사용자 입력 : 로그 파일 경로
# - IP 주소 추출 및 빈도 계산
# - 상위 3개 IP 주소 출력
# - 분석 결과 CSV 저장
#    - 파일명 : ip_analysis.csv

# --------------------------------------------------------------------------------------------------------------------------------------------

import re
import sys

# IP 주소 추출
def get_ip_addr(file_path):
    print(file_path)

# IP 빈도 계산

# 상위 3개 IP 주소 출력


# 분석 결과 CSV 저장


if __name__ == "__main__":
    file_path = sys.argv[1]
    get_ip_addr(file_path)

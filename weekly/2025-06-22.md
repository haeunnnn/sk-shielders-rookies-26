<br/>

**week** : 2 <br/>
**period** : 2025-06-16 ~ 2025-06-22 <br/>
**topics** : [데이터 수집, 파일 저장, SQLite, 데이터 전처리, 스케일링, 지도학습, 회귀분석, 분류, KNN] <br/>

<br/>

# 📚 1주차 요약

<br/>

## 🧩 전체 주제

- 웹 크롤링 및 데이터 수집 자동화
- 다양한 파일 포맷(CSV, Excel, JSON)으로 데이터 저장
- SQLite를 활용한 로컬 데이터베이스 관리
- 데이터 전처리 및 정제
- 데이터 변환 및 스케일링
- 지도학습 기반 회귀 및 분류 모델 학습
- 머신러닝 모델의 성능 평가와 개선 전략 이해

<br/>

## 📌 주요 학습 주제
- 파이썬 언어의 기초 문법 및 개발 환경
- 자료형, 제어문, 함수, 예외처리, 객체지향
- 데이터 연산 및 처리(Numpy, Pandas)
- 정규표현식, 시각화(Matplotlib, Seaborn)

### 1. 파이썬 기초 개념

#### 파이썬 특징
- 인터프리터 언어 (한 줄씩 실행)
- 동적 타이핑, 가독성 높음
- 풍부한 라이브러리 (AI, 웹, 시스템 등)

#### 변수와 자료형
- 변수는 객체의 **주소**를 참조
- 자료형 명시 없이 사용 가능

| 자료형 | 특징 |
|--------|------|
| int, float | 숫자형 |
| str | 문자열 |
| bool | True/False |
| list | 순서 O, 변경 O |
| tuple | 순서 O, 변경 X |
| dict | 키-값 쌍, 순서 X |
| set | 중복 X, 순서 X |

#### 문자열 관련 함수

| 함수 | 설명 | 예시 |
|-----|--------|------|
| `upper()` | 대문자로 변환 | `"abc".upper() → "ABC"` |
| `lower()` | 소문자로 반환 | `"ABC".lower() → "abc"` |
| `replace()` | 문자열 치환 | `"hello".replace("l", "x") → "hexxo"` |
| `split()` | 문자열 분리 → 리스트 반환 | `"a,b,c".split(",") → ['a', 'b', 'c']` |
| `strip()` | 양쪽 공백 제거 | `" hi ".strip() → "hi"` |
| `lstrip()` | 왼쪽 공백 제거 | `" hi ".strip() → "hi "` |
| `rstrip()` | 오른쪽 공백 제거  | `" hi ".strip() → " hi"` |
| `find()` | 하위 문자열의 첫 위치 반환 (없으면 -1) | `"apple".find("p") → 1` |
| `startswith()` | 특정 문자열로 시작하는지 확인 | `"hello".startswith("he") → True` |
| `endswith()` | 특정 문자열로 끝나는지 확인 | `"file.txt".endswith(".txt") → True` |
| `join()` | 시퀀스의 요소를 구분자로 연결 | `", ".join("abc") → "a, b, c"` |

### 2. 제어문과 컬렉션 자료형

#### 조건문

``` python
if 조건:  
  실행  
elif 조건2:  
  실행  
else:  
  실행
```

#### 반복문
- `for`, `while`, `break`, `continue`

#### 리스트 컴프리헨션
`[x for x in range(10) if x % 2 == 0]`

#### 컬렉션 요약

| 자료형 | 변경 가능 | 순서 | 중복 | 인덱싱 |
|--------|-----------|------|------|--------|
| list | O | O | O | O |
| tuple | X | O | O | O |
| set | O | X | X | X |
| dict | O | X | 키 중복 X | 키로 접근 |

### 3. 함수와 예외 처리

#### 함수 정의

``` python
def name(param):  
  return result
```
- `*args`, `**kwargs`: 가변 인자
- `lambda`: 익명 함수

#### 주요 내장 함수

| 함수 | 설명 |
|------|------|
| `abs()`, `round()` | 수치 함수 |
| `len()`, `type()` | 정보 확인 |
| `map()`, `filter()` | 반복/필터링 |

#### 예외 처리

``` python
try:  
  실행  
except 예외 as e:  
  처리  
finally:  
  항상 실행
```

- 사용자 정의 예외: class MyError(Exception): ...
- 예외 종류: ValueError, IndexError, TypeError, FileNotFoundError 등

### 4. 클래스, 모듈, 표준 라이브러리

#### 클래스 구성

``` python
class A:  
  def __init__(self, ...):  
    ...  
  def method(self):  
    ...
```

- self: 인스턴스 자기참조
- 상속, 오버라이딩 가능

#### 모듈/패키지

``` python
import 모듈  
from 모듈 import 함수
```

- `__name__ == "__main__"`: 직접 실행 여부 판단

#### 표준 라이브러리

| 모듈 | 주요 함수 |
|------|-----------|
| `datetime` | 날짜 관련 |
| `time` | `sleep`, `time()` |
| `random` | `random()`, `choice()`, `sample()` |
| `os` | 디렉토리, 시스템 명령 |
| `json` | 파일-객체 변환 (`load`, `dumps`) |

### 5. Numpy 활용

#### 배열 생성 및 연산

| 함수 | 기능 | 설명 |
|------|------|------|
| 인덱싱 / 슬라이싱 |  | 리스트와 동일한 방식 사용 |
| `np.array()` | 배열 생성 | 리스트나 튜플 등을 다차원 배열로 변환 |
| `np.zeros(shape)` | 배열 생성 | 모든 요소가 0인 배열 생성 |
| `np.ones(shape)` | 배열 생성 | 모든 요소가 1인 배열 생성 |
| `np.linspace(start, stop, num)` | 배열 생성 | 지정한 구간을 일정 간격으로 나눈 배열 생성 |
| `np.reshape(a, new_shape)` | 배열 형태 변경 | 배열의 형태(shape)를 변경 |
| `a.flatten()` | 배열 형태 변경 | 다차원 배열을 1차원으로 변환 |
| `np.concatenate([a, b], axis)` | 배열 결합 | 여러 배열을 지정된 축으로 연결 |
| `np.random.rand(d0, d1, ...)` | 난수 생성 | 0~1 사이의 난수를 균등 분포로 생성 |
| `np.random.randint(low, high, size)` | 난수 생성 | 지정한 범위 내 정수 난수 배열 생성 |
| `np.random.permutation(seq)` | 난수 생성 | 시퀀스나 정수 범위를 무작위로 섞은 배열 반환 |
| `np.sum(a)` | 배열 연산 | 배열의 전체 합 계산 |
| `np.mean(a)` | 배열 연산 | 배열의 평균 계산 |
| `np.dot(a, b)` | 배열 연산 | 행렬 내적 계산 (벡터/행렬 곱) |
| `np.max(a)` | 배열 연산 | 배열의 최대값 반환 |
| `np.min(a)` | 배열 연산 | 배열의 최소값 반환 |
| `np.sort(a)` | 정렬 | 배열 요소 정렬 |

#### 특징
- 빠른 연산, 벡터화, 브로드캐스팅
- AI에서 벡터 계산, 손실 계산, 데이터 전처리 등 활용

### 6. Pandas 활용

#### 기본 구조
- Series: 1차원 데이터
- DataFrame: 2차원 테이블

#### 주요 기능

| 함수 | 기능 | 설명 |
|--------------|------|------|
| `pd.Series()` | 시리즈 생성 | 1차원 데이터 구조 (인덱스 포함) 생성 |
| `pd.DataFrame()` | 데이터프레임 생성 | 2차원 행·열 형태의 데이터 생성 |
| `.head(n)` | 데이터 확인 | 앞 n행 출력 (기본 5행) |
| `.tail(n)` | 데이터 확인 | 뒤 n행 출력 (기본 5행) |
| `.shape` | 데이터 구조 | (행, 열) 형태 반환 |
| `.info()` | 구조 정보 | 데이터 타입, 결측치 여부 등 출력 |
| `.describe()` | 통계 요약 | 수치형 열의 요약 통계량 제공 |
| `df['열이름']` | 열 선택 | 특정 열 선택 (시리즈 형태 반환) |
| `df.loc[]` | 데이터 접근 | 인덱스 이름 기반 접근 |
| `df.iloc[]` | 데이터 접근 | 정수 기반 위치로 접근 |
| 조건 필터링 (`df[df['열'] 조건]`) | 행 필터링 | 조건을 만족하는 행만 선택 |
| `df['새열'] = 값` | 열 추가 | 새로운 열 생성 및 할당 |
| `pd.concat([df1, df2])` | 행 추가 | 두 데이터프레임을 행 기준으로 합치기 |
| `fillna(value)` | 결측치 처리 | NaN 값을 지정한 값으로 대체 |
| `dropna()` | 결측치 처리 | 결측치가 포함된 행 또는 열 삭제 |
| `sort_values(by)` | 정렬 | 지정한 열 기준으로 정렬 |
| `groupby('열')` | 그룹화 | 지정 열 기준으로 그룹을 나누어 연산 가능 |
| `pd.read_csv('파일경로')` | 파일 읽기 | CSV 파일을 데이터프레임으로 읽기 |
| `df.to_csv('파일경로')` | 파일 저장 | 데이터프레임을 CSV 파일로 저장 |
| `.index` | 속성 | 행 인덱스 객체 반환 |
| `.values` | 속성 | 데이터 값만 넘파이 배열로 반환 |

### 7. 정규표현식

#### 주요 함수

| 함수 | 설명 |
|------|------|
| `match()`, `search()` | 패턴 매칭 |
| `findall()` | 모든 결과 리스트 반환 |
| `compile()` | 패턴 재사용 가능 |

#### 메타 문자

| 기호 | 의미 |
|------|------|
| `.` | 임의의 문자 |
| `^`, `$` | 시작, 끝 |
| `*`, `+`, `?`, `{n,m}` | 반복 |
| `[]`, `[^]` | 문자 클래스 |
| `\d`, `\w`, `\s` | 숫자, 문자, 공백 |

### 8. 시각화 라이브러리

#### Matplotlib
- 기본 선형 그래프, 막대 그래프, 히스토그램

#### Seaborn
- 상관관계 히트맵, 박스플롯, 바이올린플롯 등 고급 시각화

<br/>

## 🧪 실습 요약

- 개발 환경 설정 : VSCode, Jupyter, 가상환경
- 파이썬 기초 문법 : 변수, 자료형, 조건문, 반복문, 함수, 예외처리
- 클래스/모듈/패키지 사용법 및 Numpy로 배열 연산
- Pandas를 이용한 데이터 분석과 정제
- 정규표현식 기반 전처리
- Matplotlib/Seaborn을 이용한 시각화

### 실습 주제 1 : 파이썬을 통해 웹 크롤링해보기
- requests로 웹 페이지 요청
- BeautifulSoup으로 HTML 파싱 및 데이터 추출
- Selenium으로 동적 웹 페이지 조작 및 데이터 수집

### 실습 주제 2 : 파이썬을 통해 수집한 데이터를 다양한 형식으로 저장해보기
- CSV, Excel, JSON으로 저장 및 불러오기
- pandas 및 json 모듈 활용

### 실습 주제 3 : 파이썬을 통해 SQLite 데이터베이스 관리해보기
- 데이터베이스 생성 및 연결
- 테이블 생성 및 CRUD 쿼리 실행

### 실습 주제 4 : 웹 서버 로그 분석
- 정규표현식으로 주요 항목 추출 후 CSV 저장 및 시각화
- `/admin` 경로 요청 탐지 및 상태코드 필터링

### 실습 주제 5 : 서울시 스타벅스 매장 데이터 분석
- 결측치 제거, 구별 매장 수 집계 및 시각화

### 실습 주제 6 : 지하철 2호선 강남역 혼잡도 분석
- 공공데이터 활용, 시간대별 평균 혼잡도 시각화

### 실습 주제 7 : 지도학습 - 선형 회귀 모델 실습
- 학생 성적 데이터셋을 활용한 수치 예측
- 전처리, 학습, 평가 (MSE, R² 등)

### 실습 주제 8 : 지도학습 - 분류 모델 실습
- 유방암/아이리스 데이터셋을 활용한 로지스틱 회귀, KNN 실습
- 성능 평가 지표(정확도, 정밀도, 재현율, F1-score) 확인

<br/>

## 🐛 에러 및 해결 사례

| 문제 | 해결 방법 |
|------|------------|
| csv 파일 인코딩 문제 | `encoding="cp949"` 또는 `encoding="euc-kr"`로 해결 |
| 해당 없음 | - |

<br/>

## 🧠 느낀 점 / 개선 아이디어

- requests/BeautifulSoup은 간단한 정적 크롤링에 적합하고, Selenium은 실제 브라우저와 유사해서 활용도가 높음 → 용도에 따라 적절히 선택해야 겠다고 느꼈다
- 다양한 저장 방식 실습이 실제 데이터 파이프라인에 어떻게 연결되는지 감을 잡는 데 유익했다
- 데이터 정제와 변환의 중요성을 체감 → 실습에서 직접 다뤄보니 모델 학습 전에 데이터를 처리하는 것이 생각한 것보다 더 중요하다는 것을 알게 되었다
- 선형 회귀 모델 학습에 사용되는 함수들을 익힐 수 있었다
- 분류 모델은 평가 지표 선택이 중요하다는 점을 느꼈고, 특히 클래스 불균형 시 정확도 외의 지표도 꼭 고려해야겠다고 느꼈다

<br/>

## 📂 관련 자료

- 📁 실습 코드
    - [`/project/2025-06-16`](../project/python/2025-06-16.ipynb)
    - [`/project/2025-06-17`](../project/python/2025-06-17.ipynb)
    - [`/project/2025-06-18`](../project/python/2025-06-18.ipynb)
    - [`/project/2025-06-19`](../project/python/2025-06-19.ipynb)
    - [`/project/2025-06-20`](../project/python/2025-06-20.ipynb)
    - [`/practice/`](../project/python/Practice/2025-06-20/)
- 🖼 스크린샷: [`/screenshots/`](../screenshots/2025-06-17/2호선_강남역_평일_시간대별_혼잡도.png)
- 📄 일일 노트
    - [`daily/2025-06-16`](../daily/python/2025-06-16.md)
    - [`daily/2025-06-17`](../daily/python/2025-06-17.md)
    - [`daily/2025-06-18`](../daily/python/2025-06-18.md)
    - [`daily/2025-06-19`](../daily/python/2025-06-19.md)
    - [`daily/2025-06-20`](../daily/python/2025-06-20.md)

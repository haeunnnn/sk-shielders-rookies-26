
<br/>

**date** : 2025-07-11 <br/>
**title** : 네트워크 보안 3<br/>
**tags** : [TCP Header, 3-way handshake, 4-way handshake, UDP Header, IP Header, MTU, 단편화] <br/>

<br/>

## 📌 오늘의 키워드

- TCP 헤더 - Source / Destination Port Number, Sequence/Acknowledge Number, Data Offset, Flags, Window Size, Checksum, Urgent Pointer, Options
- UDP 헤더 - Source / Destination Port Number, Total Length, Checksum
- IP 헤더 - Version, HLEN, Type of Service, Total Length, Identification, Flags, Fragmentation offset, TTL, Protocol, Header Checksum, Source / Destination IP Address

<br/>

## 🧠 주요 개념 정리

### 1. TCP Header
- 20 ~ 60 Bytes (가변 길이)

``` text
┌────────────────────────────────────────────┬────────────────────────────────────────────┐
│            Source Port Address             │          Destination Port Address          │
│                  16 bit                    │                   16 bit                   │
└────────────────────────────────────────────┴────────────────────────────────────────────┘
│                                   Sequence Number                                       │
│                                         32 bit                                          │               
└─────────────────────────────────────────────────────────────────────────────────────────┘
│                                Acknowledgment Number                                    │
│                                         32 bit                                          │               
└─────────────────────────────────────────────────────────────────────────────────────────┘
│   HLEN  │ Reserved │ u │ a │ p │ r │ s │ f │                 Window Size                │
│  4 bit  │  6 bit   │ r │ c │ s │ s │ y │ i │                   16 bit                   │
│         │          │ g │ k │ h │ t │ n │ n │                                            │               
└────────────────────────────────────────────┴────────────────────────────────────────────┘
│                   Checksum                 │                Urgent Pointer              │
│                    16 bit                  │                    16 bit                  │               
└────────────────────────────────────────────┴────────────────────────────────────────────┘
│                                    Options & Padding                                    │
│                                                                                         │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

#### TCP 헤더 필드 요소들

| 필드 | 설명 |
| --- | ---- |
| **Source Port / Destination Port** | 송/수신 측 포트 번호 |
| **Sequence Number** | 전송할 데이터의 시작 바이트 번호 |
| **Acknowledgment Number** | 수신 확인된 다음 바이트 번호 (Sequence Number + Data Length) |
| **Data Offset** | 헤더 길이 (4비트, 단위: 32비트 word) |
| **Flags** | 연결 관리 및 제어 비트 (SYN, ACK, FIN 등) |
| **Window Size** | 수신자가 허용 가능한 버퍼 크기 (흐름 제어용) |
| **Checksum** | 헤더 + 데이터에 대한 오류 검출 |
| **Urgent Pointer** | 긴급 데이터의 끝 위치 (URG 플래그와 함께 사용) |
| **Options** | MSS, Window Scale, Timestamps 등 확장 기능 |

#### TCP 헤더 플래그 종류

| 플래그 | 의미 | 사용 예시 |
| ----- | --- | -------- |
| **SYN** | 연결 시작 요청 | 3-Way Handshake |
| **ACK** | 응답 확인 | 연결 확인, 데이터 수신 확인 |
| **FIN** | 연결 종료 요청 | 4-Way Handshake |
| **RST** | 비정상 연결 종료 | 오류 처리 |
| **PSH** | 수신 측이 즉시 처리 | 실시간 데이터 전송 |
| **URG** | 긴급 데이터 있음 | 우선 처리 필요할 때 사용 |

#### TCP 기능

- **데이터 전송 전** : 3-way handshake
- **데이터 전송 중** : 신뢰성 확인, 오류제어, 흐름제어
- **데이터 전송 후** : 4-way handshake(4-way termination)

#### TCP 3-way handshake

``` text
               [Client]                                  [Server]
                  |                                          |
1 Closed          |                                          |     Listen
                  |                                          |
2 SYN Sent        |  ------------ SYN (seq=x) ------------>  |     
                  |                                          |     SYN Received
                  |  <----- SYN+ACK (seq=y, ack=x+1) ------  |     
                  |                                          |
3 Established     |  ----------- ACK (ack=y+1) ----------->  |     
                  |                                          |     Established
```

1. `SYN` : 클라이언트가 서버에게 연결을 요청 (시작 시퀀스 번호 `x`)
2. `SYN + ACK` : 서버가 클라이언트의 요청을 수락하며 자신도 연결 요청 (`seq = y, ack = x + 1`)
3. `ACK` : 클라이언트가 서버의 응답을 확인 (`ack = y + 1`)

- 3번의 패킷 교환을 통해 **신뢰성 있는 연결(연결지향)**을 구성
- 초기 시퀀스 번호(seq)는 무작위 값으로 설정되어 보안성 향상
- 연결이 완료되면 이후 데이터 전송(데이터 → ACK 응답)이 시작됨
- cmd 창에 `netstat -an`으로 연결 상태 확인

#### TCP 4-way handshake(terminate)

```text
               [Client]                                  [Server]
                  |                                          |
1 Established     |                                          |     Established
                  |  ------------ FIN (seq=x) ------------>  |     
                  |                                          |     Close Wait
2 FIN-WAIT-1      |                                          |
                  |  <----------- ACK (ack=x+1) -----------  |     
                  |                                          |     Close Wait
3 FIN-WAIT-2      |                                          |
                  |  <----------- FIN (seq=y) -------------  |     
                  |                                          |     Last ACK
4 TIME-WAIT       |  ------------ ACK (ack=y+1) ---------->  |     
                  |                                          |     Closed
5 (wait expires)  |                                          |
   Closed         |                                          |
```

| 단계 | 클라이언트 상태    | 서버 상태       | 설명                    |
| -- | ----------- | ----------- | --------------------- |
| 1  | ESTABLISHED | ESTABLISHED | 클라이언트가 먼저 종료 요청 (FIN) |
| 2  | FIN-WAIT-1  | CLOSE-WAIT  | 서버가 ACK 응답            |
| 3  | FIN-WAIT-2  | LAST-ACK    | 서버가 FIN 요청            |
| 4  | TIME-WAIT   | CLOSED      | 클라이언트가 ACK 응답 후 대기    |
| 5  | CLOSED      | CLOSED      | 연결 완전히 종료됨            |

#### TIME_WAIT
- 클라이언트는 마지막 ACK를 보내고 바로 연결을 끊지 않고 일정 시간 대기
- 이는 지연된 패킷이 네트워크에 남아 있을 경우 재전송을 방지하기 위한 안전장치
- 기본 시간 : `2 * Maximum Segment Lifetime (MSL)`

### 2. UDP Header
- 8 Bytes (고정 길이)

``` text
┌────────────────────────────────────────────┬────────────────────────────────────────────┐
│            Source Port Address             │          Destination Port Address          │
│                  16 bit                    │                   16 bit                   │
└────────────────────────────────────────────┴────────────────────────────────────────────┘
│                  Checksum                  │                Urgent Pointer              │
│                   16 bit                   │                    16 bit                  │               
└────────────────────────────────────────────┴────────────────────────────────────────────┘
```

### 3. IP Header
- 20 ~ 60 Bytes (가변 길이)

``` text
┌────────────────────────────────────────────┬────────────────────────────────────────────┐
│   VER   │  HLEN  │      Service Type       │                Total Length                │
│  4 bit  │  4 bit │          8 bit          │                   16 bit                   │
└────────────────────────────────────────────┴────────────────────────────────────────────┘
│               Identification               │   Flags   |      Fragmentation offset      │
│                  16 bit                    │   3 bit   |            13 bit              │
└────────────────────────────────────────────┴────────────────────────────────────────────┘
│   Time To Live    |        Protocol        │               Header Checksum              │
│      8 bit        |         8 bit          │                    16 bit                  │               
└────────────────────────────────────────────┴────────────────────────────────────────────┘
│                                   Source IP Address                                     │
│                                         32 bit                                          │               
└─────────────────────────────────────────────────────────────────────────────────────────┘
│                                Destination IP Address                                   │
│                                         32 bit                                          │               
└─────────────────────────────────────────────────────────────────────────────────────────┘
│                                    Options & Padding                                    │
│                                                                                         │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

| 필드 이름 | 크기 (bit) | 설명 |
| -------- | --------- | ---- |
| **Version** | 4 | IP 버전 (IPv4는 4) |
| **IHL (Header Length)** | 4 | IP 헤더 길이 (단위: 4바이트), 기본값 5 (20바이트) |
| **Type of Service (ToS)** | 8 | 서비스 우선순위, 지연/처리 요구도 표시 (현재는 DSCP/ECN으로 사용됨) |
| **Total Length** | 16 | IP 헤더 + 데이터 전체 길이 (최대 65,535 바이트) |
| **Identification** | 16 | 조각화된 패킷을 재조합하기 위한 식별자 |
| **Flags** | 3 | 조각화 제어 플래그 (DF: Don't Fragment, MF: More Fragment) |
| **Fragment Offset** | 13 | 조각화된 데이터의 상대 위치 |
| **Time To Live (TTL)** | 8 | 패킷 생존 시간 (라우터 통과 시 감소, 0이면 폐기) |
| **Protocol** | 8 | 상위 계층 프로토콜 식별 (TCP: 6, UDP: 17 등) |
| **Header Checksum** | 16 | IP 헤더 오류 검출용 체크섬 |
| **Source IP Address** | 32 | 발신자의 IP 주소 |
| **Destination IP Address** | 32 | 수신자의 IP 주소 |
| **Options (선택)** | 가변 | 보안, 경로 지정 등 특수 기능 (사용 안 되는 경우가 많음) |
| **Padding** | 가변 | 4바이트 정렬을 위한 채움 값 |

#### MTU(Maximum Transfer Unit)
- 한 번에 전송할 수 있는 최대 Layer 2 프레임(데이터 링크 계층)의 데이터 크기
- IP 패킷(헤더 + 데이터)은 MTU보다 클 경우 조각화(Fragmentation) 되어 전송
- MTU는 헤더를 포함한 전체 프레임 크기가 아니라, 전송 가능한 "데이터(payload)"의 최대 크기
- 기본 MTU 값 : 이더넷 기준 1500바이트 (기본값, IP 헤더 20바이트 포함 시 실제 데이터는 1480바이트)
- `DF` 비트 : Don't Fragment. 설정되면 조각화를 금지함. MTU보다 크면 라우터가 폐기하고 ICMP 오류 반환.
- MTU 설정 문제
    - MTU 설정이 잘못되면 조각화로 인한 성능 저하 발생 가능
    - 특히 VPN, GRE 터널, ICMP 차단 시 MTU 미스매치로 인해 "응답 없음", "연결 끊김" 현상 발생 가능

#### 조각화(Fragmentation)
- MTU보다 큰 IP 패킷은 조각으로 나눠 전송
- IP 헤더의 `Identification`/`Offset`/`Flags` 사용

**MTU와 조각화의 예시**
- MTU = 1500 bytes
- IP 헤더 = 20 bytes, TCP 헤더 = 20 bytes → 유효 데이터 최대: 1460 bytes
- 즉, 2000바이트를 보내면 → 1500 이하로 쪼개짐 → 2개의 조각(IP 패킷)으로 나눠 전송됨

### 4. TCP/UDP/IP

| 항목 | TCP | UDP | IP |
| ---- | ---- | ---- | --- |
| **계층** | 전송 계층 (4계층) | 전송 계층 (4계층) | 네트워크 계층 (3계층) |
| **연결 방식** | 연결지향 (Connection-Oriented) | 비연결지향 (Connectionless) | 비연결지향 |
| **신뢰성 보장** | O (수신 확인, 재전송) | X (확인 없음, 재전송 없음) | X (IP 패킷 자체는 신뢰성 없음) |
| **흐름 제어** | O (윈도우 크기 조절) | X | X |
| **오류 제어** | O (체크섬 + 재전송) | X (체크섬만 있음, 재전송 X) | O (체크섬 있음, 재전송 X) |
| **전송 방향** | 양방향 (Full Duplex) | 단방향 또는 제한적 양방향 | 단방향 (Hop-by-hop) |
| **속도** | 느림 (신뢰성 확보로 인한 오버헤드) | 빠름 (단순 구조, 실시간 전송 적합)  | 빠름 |
| **대표 용도** | 웹(HTTP), 이메일(SMTP), 파일 전송(FTP), SSH 등 | 스트리밍, VoIP, 게임, 방송 등 | 모든 IP 기반 통신의 라우팅 및 전달 |
| **데이터 단위(PDU)** | Segment | Datagram | Packet |
| **연결 설정** | 3-Way Handshake 필요 | 없음 | 없음 |
| **단편화 여부** | 자체적으로는 하지 않음 | 자체적으로는 하지 않음 | O (MTU 초과 시 조각화) |
| **주소 정보** | 포트 번호 + IP 주소 | 포트 번호 + IP 주소 | IP 주소만 사용 |

- **TCP** : 신뢰성 + 흐름/오류 제어 + 연결 필요 ⇒ 정확한 데이터 전송
- **UDP** : 빠름 + 비신뢰성 ⇒ 실시간성 중요할 때 (음성/영상)
- **IP** : 경로 지정 및 전송용 주소 지정 ⇒ 데이터가 어디로 가야 하는지 알려줌

<br/>

## 💻 실습 내용

### 실습 주제 1: Wireshark를 통해 패킷 헤더 필드 확인하기
- TCP, UDP 헤더 필드 확인하기
- IP 헤더 필드 확인하기

### 실습 주제 2: Wireshark를 통해 패킷 필터링하기
- **캡처 필터(capture filter)** : 패킷이 캡처될 때 수집 대상 패킷을 제한하여 지정된 표현식에 포함되거나 제외된 패킷만 수집되도록 함
- **디스플레이 필터(display filter)** : 수집한 패킷 중 지정된 표현식을 기반으로 원하는 패킷만 보이도록 함

<br/>

## 🐛 문제 & 해결

- 해당 없음

<br/>

## ❓ 궁금한 점

- 궁금한 점 1
- 궁금한 점 2

<br/>

## 🔗 참고 자료

- 없음 (교재/강의 기반 정리)

<br/>

## 🗂 관련 파일

📁 코드 : 해당 없음 <br/>
🖼 스크린샷 : [`/screenshots/`](../../screenshots/2025-07-11/)
